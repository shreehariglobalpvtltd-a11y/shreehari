# SHG VPS Deploy Config
# Copy this to .deploy.env and fill in your values.
# .deploy.env is in .gitignore — never commit it.

# Hostinger VPS details
VPS_USER=root
VPS_HOST=93.127.167.249
VPS_PATH=/home/u123456789/public_html

# SSH private key (generated by setup-deploy.sh)
SSH_KEY=~/.ssh/shg_deploy

# Site URL for post-deploy health check
SITE_URL=https://shreehariglobal.in
